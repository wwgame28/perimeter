meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

build:
  requirementsPath: requirements.txt
  useCache: false

run:
  scriptName: app.py
  persistenceMount: /data
  containerPort: 8080
